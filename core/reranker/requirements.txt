# reranker 小服務依賴。建議 Python 3.10+。
# Apple Silicon 用 MPS、NVIDIA 用 CUDA、其餘 CPU（程式會自動偵測）。
torch>=2.2
transformers>=4.40
sentencepiece>=0.1.99      # XLM-RoBERTa（bge-reranker-v2-m3）tokenizer 需要
# 註：只用到標準函式庫 http.server 起服務，不需 flask/fastapi。
